# 🐱 AI 貓狗影像辨識與自訂訓練系統 🐶

本專案是一個基於 PyTorch 與 ResNet-18 的電腦視覺專案，提供了一個圖形化介面 (Tkinter GUI) 讓使用者可以進行貓狗影像辨識，並內建了獨立的 AI 訓練腳本，讓使用者可以在本地端「下載數據集」並「訓練出屬於自己的 AI 分類模型」。

---

## 🌟 系統特色

1. **雙模支援 (預設模型與自訂模型)**
   - **自訂訓練模型**：啟動時若偵測到 `model.pth`，會自動載入你在本地端訓練的 2 分類模型，辨識專注於貓與狗。
   - **預設 ImageNet 模型**：若未進行訓練，系統會自動使用預設的 ImageNet 預訓練權重（合併 ImageNet 中上百種貓科與犬科亞種的機率）進行高精度辨識。

2. **強健的雙源下載機制**
   - 內建非同步下載功能，優先下載由 Google 託管的精簡版數據集（~68MB）。
   - 若偵測到下載受限（如 GCS 403 錯誤），會自動切換至 fast.ai 備用伺服器下載完整版數據集（~839MB）並自動解壓縮，確保下載 100% 成功。

3. **遷移學習微調 (Transfer Learning)**
   - 訓練腳本採用 ResNet-18 遷移學習技術，凍結特徵提取層，僅訓練最後的分類器。
   - 訓練速度極快，即使在 CPU 上也只需數分鐘，且準確度可達 95% 以上。

---

## 🛠️ 環境需求與安裝

本系統需要以下 Python 套件支援：

- Python 3.8+
- PyTorch (建議安裝支援 CUDA 的版本以加速訓練)
- Torchvision
- Pillow (PIL)
- Tkinter (Python 內建圖形庫)

### 安裝指令

請在終端機中執行以下指令進行套件安裝：

```bash
pip install torch torchvision pillow
```

---

## 📂 專案檔案結構

```text
機電整合/
├── try.py              # Tkinter GUI 影像分類辨識主程式
├── train.py            # AI 模型訓練微調腳本 (訓練後產生 model.pth)
├── README              # 專案說明文件 (本檔案)
├── model.pth           # 自訂訓練完成的 AI 模型權重檔 (自動生成)
└── kaggle_dataset/     # 自動下載並解壓縮的貓狗照片數據集 (自動生成)
```

---

## 🚀 使用指南

### 1. 啟動影像辨識 GUI 系統

在專案目錄下執行：

```bash
python try.py
```
- 啟動後，下方的狀態欄會提示目前載入的模型狀態。
- 點選「選擇圖片」選取貓狗照片後，點擊「開始辨識」即可看到辨識結果與信心度。

---

### 2. 訓練你自己的 AI 模型

如果你想使用 Kaggle 貓狗照片訓練一個專屬於你的 AI 模型，請在終端機中執行：

```bash
python train.py
```
- **自動化準備**：程式會自動檢查並下載數據集。
- **開始訓練**：程式會自動將模型設定為「微調模式」並進行 3 個 Epoch 的訓練，期間會輸出 Train/Val 的 Loss 與 Accuracy 進度。
- **儲存權重**：訓練完成後會自動在同目錄下生成 `model.pth`。
- **啟用**：完成後重新執行 `python try.py`，系統將會顯示 `✅ 自訂訓練模型已載入！`，即可開始使用你的專屬 AI！
