# 🐱 AI 貓狗影像辨識與定位系統 🐶

本專案是一個基於 PyTorch/ResNet-18 分類模型與 Ultralytics/YOLOv8 物件偵測模型的電腦視覺專案。本專案提供了一個直觀的圖形化介面 (Tkinter GUI)，除了解析圖片分類，更支援在畫面上「自動框選定位 (Bounding Box) 物體」，並內建本地端 AI 訓練模組。

---

## 🌟 系統特色

1. **三種模型模式自由切換 (選單控制)**
   - **YOLOv8 (物件偵測)**：整合 YOLOv8n，辨識時會自動在圖片預覽區**繪製彩色定位框與置信度標籤**。支援同時辨識多個物件（如人、貓、狗等），並將標籤翻譯為繁體中文。
   - **ResNet-18 (自訂訓練分類器)**：啟動時若偵測到本地端訓練的 `model.pth`，會自動載入此模型，辨識專注於貓與狗（2 分類）。
   - **ResNet-18 (預設 ImageNet 分類器)**：若無 `model.pth`，自動使用預設的 ImageNet 預訓練權重進行高精度 1000 類別辨識。

2. **強健的雙源下載機制**
   - 內建非同步下載功能，優先下載由 Google 託管的精簡版數據集（~68MB）。
   - 若偵測到下載受限（如 GCS 403 錯誤），會自動切換至 fast.ai 備用伺服器下載完整版數據集（~839MB）並自動解壓縮，確保數據集 100% 下載成功。

3. **遷移學習微調 (Transfer Learning)**
   - 訓練腳本採用 ResNet-18 遷移學習技術，凍結特徵提取層，僅訓練最後的分類器。
   - 訓練速度極快，即使在 CPU 上也只需數分鐘，且準確度可達 95% 以上。

---

## 🛠️ 環境需求與安裝

本系統需要以下 Python 套件支援：

- Python 3.8+
- PyTorch (建議安裝支援 CUDA 的版本以加速訓練)
- Torchvision
- Pillow (PIL)
- Ultralytics (提供 YOLOv8 物件偵測支援)
- Tkinter (Python 內建圖形庫)

### 安裝指令

請在終端機中執行以下指令進行套件安裝：

```bash
pip install torch torchvision pillow ultralytics
```

---

## 📂 專案檔案結構

```text
機電整合/
├── try.py              # Tkinter GUI 影像分類、偵測與定位主程式
├── train.py            # AI 模型訓練微調腳本 (訓練後產生 model.pth)
├── README              # 專案說明文件 (本檔案)
├── model.pth           # 自訂訓練完成的 AI 模型權重檔 (自動生成)
└── kaggle_dataset/     # 自動下載並解壓縮的貓狗照片數據集 (自動生成)
```

---

## 🚀 使用指南

### 1. 啟動影像辨識 GUI 系統

在專案目錄下執行：

```bash
python try.py
```
- **切換模式**：在視窗上方的「選擇辨識模型」下拉選單中切換 `ResNet-18` 或 `YOLOv8 (物件偵測)`。
- **偵測與辨識**：點選「選擇圖片」選取照片後，點擊「開始辨識」即可看到結果。
  - **YOLOv8 模式**：會直接在預覽圖片上繪製定位框，並統計畫面中的動物與物體數量。
  - **ResNet-18 模式**：會還原原圖，顯示分類類別與置信度進度條。

---

### 2. 訓練你自己的 AI 模型 (ResNet-18)

如果你想使用 Kaggle 貓狗照片訓練一個專屬於你的 AI 分類模型，請在終端機中執行：

```bash
python train.py
```
- **自動化準備**：程式會自動檢查並下載數據集。
- **開始訓練**：程式會自動將模型設定為「微調模式」並進行 3 個 Epoch 的訓練，期間會輸出 Train/Val 的 Loss 與 Accuracy 進度。
- **儲存權重**：訓練完成後會自動在同目錄下生成 `model.pth`。
- **啟用**：完成後重新執行 `python try.py`，當你選擇 `ResNet-18` 時，系統將會顯示 `✅ 自訂訓練模型已載入！`，即可開始使用你的專屬分類 AI！
